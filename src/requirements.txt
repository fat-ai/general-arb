# Polymarket live-trading bot — runtime dependencies.
#
# Pinned to the set validated together on 2026-06-13: installs with no resolver
# conflict (`pip check` clean), every module imports, and config/data/reporting
# import + the pandas-sensitive reporting ops run under pandas 3.0 / numpy 2.4.
# Dockerfile.bot installs exactly this set (keep the two in sync).
#
# Changes from the previous file:
#   - REMOVED py_clob_client_v2 (replaced by the unified SDK below).
#   - ADDED the strategy stack (duckdb/polars/statsmodels/numba) — main_2.py
#     imports sim_strat_5, which imports these at module load, so they are
#     required at live runtime, not just for backtests.
#   - ADDED google-cloud-secret-manager (key + relayer creds at startup).
#   - DROPPED the unused `websockets` (plural); ws_handler uses websocket-client.

# --- core bot runtime (main_2, broker, data, reporting, ws_handler) ---
aiohttp==3.14.1
numpy==2.4.6
pandas==3.0.3
pyarrow==24.0.0
requests==2.34.2
tabulate==0.10.0
websocket-client==1.9.0

# --- strategy brain: main_2 imports sim_strat_5, which imports these at load ---
duckdb==1.5.3
polars==1.41.2
statsmodels==0.14.6
numba==0.65.1

# --- GCP Secret Manager: EOA key + relayer credentials fetched at startup ---
google-cloud-secret-manager==2.29.0

# --- Polymarket unified SDK (deposit-wallet / gasless model) ---
# Pre-release; the exact pin lets pip install it without --pre.
polymarket-client==0.1.0b4
